<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TEST</title>
    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
    <style>
        @import url("https://cdn.jsdelivr.net/gh/orioncactus/pretendard@v1.3.6/dist/web/static/pretendard.css");
        * {
            box-sizing: border-box;
            font-family: "Pretendard Variable", Pretendard, -apple-system, BlinkMacSystemFont, system-ui, Roboto, "Helvetica Neue", "Segoe UI", "Apple SD Gothic Neo", "Noto Sans KR", "Malgun Gothic", "Apple Color Emoji", "Segoe UI Emoji", "Segoe UI Symbol", sans-serif;
        }
        span:not(.b) {
            position: absolute;
            left: 50px;
        }
        input {
            padding: 8px;
            outline: none;
            width: 100%;
        }
    </style>
</head>
<body>
    <div class="container">
        <h2>나는 누구랑 많이 했을까? - 발로란트</h2>
        <input id="search" type="text" placeholder="이름#태그">
    </div>
    <script>
        $("#search").keydown(e => {
            if (e.keyCode === 13) {
                $("p").remove();
                $(".container").append('<h4>조금만 기다려주세요</h4>');
                $.ajax({
                    type: 'POST',
                    url: '/api/search',
                    data: {
                        name: $("#search").val()
                    }
                }).done(e => {
                    $("h4").remove();
                    if (e == "err") {
                        $(".container").append(`<p>알 수 없는 유저명입니다.</p>`);
                    }
                    else {
                        for (let i = 0; i < e.length; i++) {
                        
                            $(".container").append(`<p><span class='b'>${e[i].count}</span><span>${e[i].user}</span></p>`);
                        }
                    }
                });
            }
        });
    </script>
</body>
</html>